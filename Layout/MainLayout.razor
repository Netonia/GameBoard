@inherits LayoutComponentBase

<div class="min-h-screen bg-gray-50">
    <!-- Navigation Header -->
    <header class="bg-white shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <h1 class="text-xl font-bold text-gray-900">🎮 GameBoard</h1>
                </div>
                <nav class="hidden md:flex space-x-8">
                    <a href="/" class="text-gray-500 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                        Accueil
                    </a>
                    @* <a href="/games" class="text-gray-500 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                        Jeux
                    </a>
                    <a href="/dashboard" class="text-gray-500 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                        Dashboard
                    </a> *@
                </nav>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="py-8">
        @Body
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
            <div class="text-center text-sm text-gray-500">
                © 2024 GameBoard - Plateforme de gestion de jeux vidéo
            </div>
        </div>
    </footer>
</div>
